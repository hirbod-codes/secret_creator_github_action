name: "Hello World"
description: "Greet someone and record the time"

inputs:
    remove-previous-swarm-secrets:
        description: boolean
        required: false
    swarm-secrets-prefix:
        description: string
        default: SWARM_
        required: false
    ssh-server-address:
        description: string
        required: false
    ssh-server-port:
        description: string
        required: false
    ssh-server-username:
        description: string
        required: false
    ssh-server-password:
        description: string
        required: false

runs:
    using: "node20"
    main: "dist/index.js"
